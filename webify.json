{
  "build": {
    "command": "pnpm build",
    "outputDir": "dist",
    "installCommand": "pnpm install"
  },
  "routes": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}